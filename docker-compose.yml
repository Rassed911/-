

services:
  backend:
    build: .
    container_name: marketing_api_service
    ports:
      - "8000:8000"
  frontend:
    build: .
    container_name: marketing_ui_service
    command: streamlit run streamlit_app.py --server.port 8501 --server.address 0.0.0.0
    ports:
      - "8501:8501"
    depends_on:
      - backend
